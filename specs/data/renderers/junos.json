{
  "vendor_os": "junos",
  "verified": false,
  "note": "Seed renderer templates pending lab syntax evidence (OI-0171).",
  "wrappers": { "enter_config": ["configure private"], "exit_config": ["commit check", "commit confirmed 2"] },
  "features": {
    "vlan": { "commands": ["set vlans {name} vlan-id {vlan_id}"] },
    "svi": { "commands": ["set interfaces irb unit {vlan_id} family inet address {address}", "set vlans {name} l3-interface irb.{vlan_id}"] },
    "trunk": { "commands": ["set interfaces {interface} unit 0 family ethernet-switching interface-mode trunk", "set interfaces {interface} unit 0 family ethernet-switching vlan members [{allowed_vlans_csv}]"] },
    "access": { "commands": ["set interfaces {interface} unit 0 family ethernet-switching interface-mode access", "set interfaces {interface} unit 0 family ethernet-switching vlan members {vlan_id}"] }
  }
}
